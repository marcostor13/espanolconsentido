# MongoDB
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/consentido

# WhatsApp (botón flotante) - número en formato internacional E.164 sin "+" ni espacios (ej. 51987654321)
VITE_WHATSAPP_NUMBER=

# Admin (para confirmar pagos - legacy, se mantiene por compatibilidad)
ADMIN_TOKEN=tu_token_secreto

# Auth (plataforma con login JWT)
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# Google Calendar
GOOGLE_CALENDAR_ID=xxx@group.calendar.google.com
GOOGLE_SERVICE_ACCOUNT_EMAIL=xxx@xxx.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Email (Resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxx
EMAIL_FROM=no-reply@espanolconsentido.com
ADMIN_NOTIFY_EMAIL=marcostor13@gmail.com

# URL pública del sitio (se usa en el enlace de login del correo de bienvenida)
SITE_URL=https://espanolconsentido.com

# S3 (subida de material de clase: PDF, video, audio, imágenes)
# 1. Crea un bucket en AWS S3 (o compatible: Cloudflare R2, Backblaze B2, etc.)
# 2. Crea un usuario/API key con permiso s3:PutObject sobre ese bucket
# 3. Configura el bucket con acceso de lectura pública para el prefijo "materials/"
#    (o usa CloudFront/S3_PUBLIC_URL_BASE si prefieres servirlo por un dominio propio)
# Nota: se usa el sufijo "2" porque Netlify reserva AWS_ACCESS_KEY_ID,
# AWS_SECRET_ACCESS_KEY, AWS_REGION y S3_BUCKET_NAME para su propio runtime.
AWS_ACCESS_KEY_ID2=tu_access_key_id
AWS_SECRET_ACCESS_KEY2=tu_secret_access_key
AWS_REGION2=us-east-1
S3_BUCKET_NAME2=espanolconsentido-materiales
# Opcional: dominio propio o de CloudFront delante del bucket
S3_PUBLIC_URL_BASE=

# PayPal (checkout real vía Orders API v2, confirmación automática)
# 1. Crea una app en https://developer.paypal.com/dashboard/applications (modo Sandbox para probar)
# 2. Copia el Client ID y el Secret de esa app
# 3. Crea un webhook (misma app > Add Webhook) apuntando a:
#    https://tu-dominio.com/api/paypal-webhook
#    con el evento "CHECKOUT.ORDER.APPROVED", y copia el Webhook ID
PAYPAL_CLIENT_ID=<tu-client-id-de-paypal>
PAYPAL_CLIENT_SECRET=<tu-client-secret-de-paypal>
PAYPAL_WEBHOOK_ID=<tu-webhook-id-de-paypal>
# "sandbox" para pruebas, "live" para cobros reales
PAYPAL_ENV=sandbox

# Wise: no ofrece una API de checkout self-service para negocios pequeños,
# así que se integra como enlace de pago manual. Los enlaces por paquete se
# configuran desde /admin/configuraciones, no aquí.
